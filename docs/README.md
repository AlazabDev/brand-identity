# Production hardening documents

- `production-db-check.sql` — read-only verification against the actual Supabase database.
- `production-external-tests.md` — live server-side release gate for Daftra, Magicplan and MinIO.

Repository-level release blockers and security remediation are tracked in `PRODUCTION_READINESS.md` and `SECURITY-NOTE.md` at the repository root.
